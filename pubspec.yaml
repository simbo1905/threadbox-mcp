name: threadbox_mcp
description: A virtual file system for agent artefacts. MCP server providing sandboxed, versioned filesystem organized by Git worktree.
version: 0.1.0
repository: https://github.com/simbo1905/threadbox-mcp

environment:
  sdk: ^3.9.2

dependencies:
  dart_mcp: ^0.3.3
  sqlite_async: ^0.3.0
  uuid: ^4.5.1
  archive: ^3.4.0
  path: ^1.9.0
  args: ^2.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  stream_channel: ^2.1.2

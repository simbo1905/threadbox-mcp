name: threadbox_mcp
description: Virtual filesystem for AI agent artefacts. Dart MCP server providing isolated, versioned sandboxes per Git worktree.
version: 0.1.0
repository: https://github.com/simbo1905/threadbox-mcp

environment:
  sdk: ^3.9.2

dependencies:
  dart_mcp: ^0.3.3
  sqlite_async: ^0.13.0
  archive: ^3.6.1
  path: ^1.9.0
  uuid: ^4.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  stream_channel: ^2.1.2
